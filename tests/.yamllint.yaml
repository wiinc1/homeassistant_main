---
extends: default

rules:
  indentation:
    spaces: 2
    indent-sequences: consistent
  document-start:
    present: false
  line-length: disable
  trailing-spaces: enable
  new-line-at-end-of-file: enable
  empty-lines:
    max: 1
    max-start: 0
    max-end: 0